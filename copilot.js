// q. what can i use instead of an index for a key in react?
// a. use the index as a key only if the list is static and will never change
// a. if the list is dynamic, use a unique id for the key
// a. if the list is static, but you want to use a unique id, use the index as a key
// a. if the list is static, but you want to use a unique id, use the index as a key
// q. how can i create a unique id?
// a. use the uuid package
// q. how do i do that?
